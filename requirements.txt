# OpenMontage - Core Dependencies
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
numpy>=1.24
requests>=2.31
google-auth>=2.0       # service-account auth for Google TTS + Imagen (Vertex AI)
google-genai>=1.0.0    # Gemini Developer API clients for Lyria, Veo, and Gemini Omni
openai>=2.44.0         # Videos API support for Sora 2

# Backlot - the living storyboard (local board server)
fastapi>=0.110
uvicorn>=0.29
watchfiles>=0.21

# Subscription-backed providers (runtime=browser) - OPTIONAL.
# Install only if you want to generate on an account you already pay for
# instead of an API key. See docs/PROVIDERS.md -> No-API-Key Providers.
#   playwright>=1.40             # gemini_web_image: drives a logged-in Gemini session
#                                #   then run: python -m playwright install chromium
#   opencv-contrib-python>=4.9   # gemini_web_image: FSR watermark removal (best quality)
#   simple-lama-inpainting>=0.1  # gemini_web_image: LaMa watermark removal (pulls torch)
# dreamina_video needs no Python package - just the `dreamina` CLI on PATH.
